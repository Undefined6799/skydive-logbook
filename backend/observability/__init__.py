"""Cross-cutting observability primitives.

Currently houses the structured-logging contract (D27). Future additions —
metrics, tracing — would sit alongside here rather than leak into the
service or API layers.
"""
